# AndroidX를 사용하겠다는 설정입니다.
android.useAndroidX=true
# 기존 라이브러리들을 AndroidX와 호환되게 자동으로 변환합니다.
android.enableJetifier=true

# 빌드 속도 향상을 위한 옵션들입니다.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
